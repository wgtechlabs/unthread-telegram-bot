# pnpm configuration
# Enforce strict engine requirements
engine-strict=true

# Save exact versions (no semver ranges)
save-exact=true

# Disable shamefully-hoist to maintain strict dependency isolation
shamefully-hoist=false

# Auto-install peers
auto-install-peers=true

# Enforce strict peer dependencies
strict-peer-dependencies=true
